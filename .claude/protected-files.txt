# MHJ scope-guard 보호 파일 목록
#
# 형식: 경로 조각 한 줄에 하나. 빈 줄과 '#' 로 시작하는 줄은 무시된다.
# 매칭 방식: .claude/hooks/scope-guard.sh 가 Edit/Write 대상 경로에 대해
#            [[ "$TARGET" == *"$line"* ]] 부분문자열 비교를 한다.
# ⚠️ 줄 끝에 인라인 주석을 달면 그 주석까지 패턴에 포함되어 절대 매칭되지 않는다.
#    설명은 반드시 위쪽 '#' 줄에 쓸 것.

# Welcome Sequence — 완료, 발송 중. 수정하려면 이 줄을 주석 처리 후 작업하고 되돌릴 것
lib/welcome-emails.ts
app/api/subscribe/route.ts
app/api/process-welcome-sequence/route.ts
vercel.json

# 구독자 관리 — 완료
app/mhj-desk/subscribers/

# 뉴스레터 admin — 원래 'newsletters/'(복수) 로 적혀 있어 실제 디렉터리
# 'newsletter/'(단수) 와 달라 수개월간 매칭된 적이 없다. 경로는 아래가 맞다.
# 다만 2026-08 현재 활발히 수정 중인 영역이라 활성화하지 않는다.
# 보호가 필요해지면 아래 줄의 '# ' 만 지울 것.
# app/mhj-desk/newsletter/

# 해제됨 (브랜드 적용으로 보호 불필요)
# components/Footer.tsx
# app/mhj-desk/blogs/
